spring:
  main:
    banner-mode: off
  datasource:
    url: jdbc:mysql://localhost:3306/recruitment_app?useSSL=false&serverTimezone=UTC
    username: utestudent
    password: utestudent
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
      properties:
        hibernate.dialect: org.hibernate.dialect.MySQL8Dialect